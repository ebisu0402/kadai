<!-- app/views/articles/show.html.erb -->
<h1>投稿詳細</h1>

<p>
    タイトル：
    <%= @article.title %><br>
    投稿内容：
    <%= @article.content %>
</p>

<%= link_to '編集する', edit_article_path(@article) %> |
<%= link_to '一覧に戻る', articles_path %> |
<%= button_to '削除する', article_path(@article), method: :delete, data: { confirm: '本当に削除しますか？' } %>
